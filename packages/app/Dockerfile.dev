# Dockerfile for React App using Vite in Production
FROM node:18-alpine

# Set working directory
WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy the source code
COPY . .

# Expose vite development server port
EXPOSE 5173

# Run the app for development
CMD ["npm", "run", "dev"]

